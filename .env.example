PORT=3004
DB_PATH=./database/timeless-radio.db

# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_PASSWORD=CHANGE_ME_use_a_long_random_secret

# Restrict CORS to your frontend domain in production (comma-separated, or * for dev)
CORS_ORIGIN=*
